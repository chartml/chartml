{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build:packages && cd docs && npm install && npm run docs:build",
  "outputDirectory": "docs/docs/.vitepress/dist",
  "installCommand": "npm install --prefix packages/core && npm install --prefix packages/chart-pie && npm install --prefix packages/chart-scatter && npm install --prefix packages/chart-metric && npm install --prefix packages/react && npm install --prefix packages/markdown-common && npm install --prefix docs",
  "framework": null,
  "devCommand": "cd docs && npm run docs:dev",
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/:path*"
    }
  ]
}
